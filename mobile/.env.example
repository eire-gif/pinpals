# Copy to .env and fill in. Metro inlines these into the bundle at build time,
# so only ever put public values here — never the service-role key, the Stripe
# secret key, or the VAPID private key.
#
# After changing this file, restart Metro with: npx expo start --clear

EXPO_PUBLIC_SUPABASE_URL=https://cicluiabimxklgmpmxmn.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
EXPO_PUBLIC_SITE_URL=https://www.pinpals.ie
